{
  "name": "formerly",
  "version": "0.1.0",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": ""
  },
  "size-limit": [
    {
      "path": "dist/index.js",
      "limit": "850 B"
    }
  ],
  "browserslist": ["chrome >= 6", "firefox >= 21", "ie >= 11"],
  "scripts": {
    "start": "jest --watch",
    "build": "tsc",
    "test": "jest",
    "lint": "tslint --project .",
    "lint:fix": "npm run lint -- --fix",
    "size": "size-limit",
    "compat": "eslint ./dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@size-limit/preset-small-lib": "^2.2.3",
    "@types/jest": "^24.0.23",
    "eslint": "^6.8.0",
    "eslint-plugin-compat": "^3.3.0",
    "jest": "^24.9.0",
    "ts-jest": "^24.2.0",
    "tslint": "^5.20.1",
    "tslint-config-airbnb": "^5.11.2",
    "typescript": "3.7.3"
  }
}
